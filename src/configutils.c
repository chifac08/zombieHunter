/**
 * Reading and writing configuration and settings for service
*/
#include <stdio.h>
#include <stdlib.h>
#include <configutils.h>

void parseConfig(char* cpConfigFile)
{
    FILE* fpConfig = NULL;
    
    //fpConfig = fopen(cpConfigFile)
}